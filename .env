# Add at the top of your script
from dotenv import load_dotenv
load_dotenv()
import os

# Then update the email_config section
self.email_config = {
    "sender_email": os.getenv("mithigithub@gmail.com"),
    "sender_password": os.getenv("mithihub"),
    "recipient_email": os.getenv("mithileshkumarrnaik@gmail.com"),
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587
}
